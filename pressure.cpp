
void* worker(void* argv)
{
	
}

class PressureRunner
{

};

int entry(int argc, char* argv)
{
	//创建N个线程，每个线程循环请求服务器M次
	//长连接，短连接
	//

	int threadNum = argv[1];	
	int requestCount = argv[2];	

	for (int i = 0; i < threadNum; ++i)
	{
	}
	//创建N个线程
	//启动线程
}
